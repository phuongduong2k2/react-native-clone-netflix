type MovieItemProps = {
  id: number;
  name: string;
  description: string;
  starring: string;
  portrait: string;
  landscape: string;
  releasedYear: string;
  trailerId: string;
};

export type {MovieItemProps};
